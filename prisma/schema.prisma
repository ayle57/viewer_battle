generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

// Real models start here. First one lands with the chat vertical slice
// (Session, ChatMessage) — see AGENTS.md for the Phase 0 spike this
// replaced.
